<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Workout Tracker</title>
  <!-- BootStrap -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha2/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-aFq/bzH65dt+w6FI2ooMVUpc+21e0SRygnTpmBvdBgSdnuTN7QbdgL+OapgHtvPp" crossorigin="anonymous">
    <!-- CSS -->
  <link rel="stylesheet" type="text/css" href= "/styles.css"/>
</head>

<body>
  <!-- navbar -->
  <nav class="navbar navbar-expand-lg navbar-dark bg-secondary border-2 border-bottom border-width">
    <div class="container-fluid">
      <a class="navbar-brand text-warning"><img src="/Workout Tracker.png" alt="logo" width="95" height="95"></a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
        aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <li class="nav-item">
            <% if (title === "index") { %>
              <a class="nav-link active" aria-current="page" href="/">Home</a>
            <% } else { %>
              <a class="nav-link" href="/">Home</a>
            <% } %>
          </li>
          <li class="nav-item">
            <% if (title === "randomWorkout") { %>
              <a class="nav-link active" aria-current="page" href="/randomWorkout">Random Workout</a>
            <% } else { %>
              <a class="nav-link" href="/randomWorkout">Random Workout</a>
            <% } %>
          </li>
          <li class="nav-item">
            <% if (title === "contact") { %>
              <a class="nav-link active" aria-current="page" href="/contact">Contact</a>
            <% } else { %>
              <a class="nav-link" href="/contact">Contact</a>
            <% } %>
          </li>
          <% if (title === "workoutLog") { %>
          <li class="nav-item">
            <a class="nav-link active" aria-current="page" href="/">Log Out</a>
          </li>
          <% } %>
        </ul>
      </div>
    </div>
  </nav>

  <!-- end navbar -->